_format_version: "3.0"

services:
  - name: supabase-api
    url: https://crud-rest-supabase.onrender.com

    routes:
      - name: supabase-route
        paths:
          - /api/v1
        strip_path: false
        methods:
          - GET
          - POST
          - PUT
          - DELETE
